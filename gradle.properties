# Minecraft / Fabric / Loom / Mappings versions
org.gradle.jvmargs=-Xmx2G
minecraft_version=1.21.8
loader_version=0.16.14            # adjust if your loader is different
fabric_api_version=0.130.0+1.21.8  # example, verify actual latest
loom_version=1.11                 # or the version needed
yarn_mappings=1.21.8+build.2       # if this version actually exists; see note below

# Project‑specific metadata
maven_group=com.yourname.modid     # change this (your package / group)
mod_version=1.0.0                   # your version
archives_base_name=voygarcompanion  # name of the mod jar
